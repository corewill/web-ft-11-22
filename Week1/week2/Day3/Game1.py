class Character:
    def __init__ (self, engage, att, dfn, runaway, health):
        self.att 
        self.dfn
        self.runaway
        self.health

    def hit(self, att):
        while True:
            if self.dodge >= attack:
                print("dodged attack")
                return False
            newHealth = self.defense - attack
            print(f'your health is{newHealth}')
            if  newHealth <= 0:
                print('wasted')
                return False

    def block(self, dfn):
         while True:
            if self.dodge >= attack:
                print("dodged attack")
                return False
            newHealth = self.defense - attack
            print(f'your health is{newHealth}')
            if  newHealth <= 0:
                print('wasted')
                return False

    def disingage(self, runaway):
